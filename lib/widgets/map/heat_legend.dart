import 'package:flutter/material.dart';

class HeatLegend extends StatelessWidget {
  const HeatLegend({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Heat Map Legend', style: TextStyle(fontSize: 16));
  }
}
